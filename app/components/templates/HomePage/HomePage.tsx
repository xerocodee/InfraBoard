import styles from './HomePage.module.scss';

const HomePage = () => (
  <div className={styles.content}>
    <span className={styles.author}>Welcome to InfraBoard</span>
  </div>
);

export default HomePage;
